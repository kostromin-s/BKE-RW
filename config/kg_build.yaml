defaults:
  - _self_
  - experiment: coad

data:
  gene_term: data_set/ontology_network/ontology_network.tsv
  ppi: data_set/ppi_network/HIPPIE.tsv

# 1. build the knowledge graph
# 2. train the model
# 3. build the knowledge graph and train the model
mode: 3

output:
  path: KGs/kg.csv